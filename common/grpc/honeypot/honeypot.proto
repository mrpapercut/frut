syntax = "proto3";

option go_package = "github.com/mrpapercut/frut/common/grpc/honeypot";

package honeypot;

service Honeypot {
    rpc GetTask(HoneypotID) returns (Task) {}
}

message HoneypotID {
    int32 id = 1;
}

message Task {
    string action = 1;
}
